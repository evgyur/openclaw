---
name: bigrestart
description: Перезапуск демона Clawdbot через systemd
metadata: {"clawdbot":{"always":true,"os":["linux"]}}
---

# Команда /bigrestart

Этот skill позволяет перезапустить демон Clawdbot через systemd, выполняя команду `systemctl --user restart clawdbot-gateway`.

## Использование

Когда пользователь отправляет команду `/bigrestart` в Telegram, выполни скрипт:

```bash
$CLAWD_WORKSPACE/clawdbot-bigrestart.sh
```

## Что делает скрипт

1. Выполняет `systemctl --user restart clawdbot-gateway`
2. Ждет 2 секунды
3. Проверяет статус демона через `systemctl --user is-active clawdbot-gateway`
4. Выводит результат с информацией о статусе

## Важно

- После перезапуска демон автоматически запустится (благодаря `Restart=always` в systemd)
- Бот может не ответить сразу после выполнения команды, так как процесс перезапускается
- Через несколько секунд бот снова будет доступен
- Скрипт находится в `{baseDir}/../clawdbot-bigrestart.sh`

## Пример ответа

После выполнения команды `/bigrestart`, сообщи пользователю:

"🔄 Перезапускаю демон Clawdbot... [выполняю скрипт]"

Если скрипт выполнен успешно:
"✅ Демон успешно перезапущен! Clawdbot снова доступен."

Если была ошибка:
"❌ Ошибка при перезапуске. Проверь логи: `journalctl --user -u clawdbot-gateway -n 50`"
