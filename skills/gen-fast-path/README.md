# Gen Fast Path ⚡

Оптимизация `/gen` команды для мгновенного отклика (<500ms) путём патча Clawdbot core.

## TL;DR

```bash
cd ~/clawd/skills/gen-fast-path
bash scripts/apply-patch.sh
```

Результат: `/gen` команда отвечает в 10-16x быстрее (5-8s → <500ms)

---

## Проблема

**До оптимизации:**
- User: `/gen` → ждёт 5-8 секунд
- Выбрали категорию → ждёт 5-8 секунд  
- Выбрали модель → ждёт 5-8 секунд

**Причина:** Каждый клик проходит через Claude (inference ~5-8s)

**После оптимизации:**
- User: `/gen` → кнопки **мгновенно** (<500ms)
- Выбор категории → **мгновенно**
- Выбор модели → **мгновенно**

---

## Как работает

### Архитектура

**До:**
```
Telegram → Clawdbot → Claude (5-8s) → router.py → Claude (5-8s) → Telegram
```

**После:**
```
Telegram → Clawdbot Fast Path → Кеш JSON → Telegram (<500ms)
                              ↓
                           Claude (только для генерации)
```

### Реализация

1. **Патчится** `bot-handlers.js` в Clawdbot core
2. **Добавляется** функция `isGenCommand()` для детекции UI команд
3. **Загружается** кеш кнопок из JSON файла
4. **Перехватываются** callback queries до попадания в Claude
5. **Отправляются** кешированные ответы напрямую через Telegram API

---

## Установка

### Требования

- Clawdbot установлен через npm
- Доступ к `~/.nvm/versions/node/.../clawdbot/dist/telegram/bot-handlers.js`
- Skill `chip-fai` для генерации изображений

### Quick Start

```bash
cd ~/clawd/skills/gen-fast-path
bash scripts/apply-patch.sh
```

Скрипт:
1. Создаст бэкап оригинального файла
2. Применит патч к Clawdbot
3. Установит кеш кнопок
4. Перезапустит Clawdbot

### Проверка

```bash
bash scripts/verify.sh
```

Покажет статус установки и кеша.

---

## Обслуживание

### При добавлении моделей в chip-fai

```bash
cd ~/clawd/skills/gen-fast-path
bash scripts/update-cache.sh
systemctl --user restart clawdbot-gateway
```

### При обновлении Clawdbot

После `npm install -g clawdbot@latest`:

```bash
cd ~/clawd/skills/gen-fast-path
bash scripts/apply-patch.sh
```

(Патч будет перезаписан при обновлении npm пакета)

### Откат

```bash
cd ~/clawd/skills/gen-fast-path
bash scripts/rollback.sh
```

Восстанавливает оригинальный `bot-handlers.js`.

---

## Структура

```
gen-fast-path/
├── README.md                   # Документация для GitHub
├── SKILL.md                    # Метаданные для Clawdbot
├── scripts/
│   ├── apply-patch.sh          # Применить патч (основная установка)
│   ├── update-cache.sh         # Обновить кеш моделей
│   ├── rollback.sh             # Откатить патч
│   └── verify.sh               # Проверить установку
└── cache/
    └── gen-cache.json          # Кеш кнопок (23 команды)
```

---

## Что кешируется

### UI Команды (23 total)

- `/gen` — стартовая команда
- `category:create`, `category:edit`, `category:enhance` — выбор категории
- `back:category` — навигация назад
- `model:*` — 18 моделей:
  - Create: nanobana-create, flux-pro, recraft, wan25-create
  - Edit: gemini, nanobana, kontext-max, seedream-v4, flux, wan25, changebg
  - Enhance: retouch, photorestore, faceenhance, realism, textremove, jpegfix, upscale

### Что НЕ кешируется

- Текстовые промпты (генерация изображений)
- Загрузка изображений
- Любые другие команды Clawdbot

---

## Performance

### Метрики

| Команда | До патча | После патча | Улучшение |
|---------|----------|-------------|-----------|
| `/gen` | 5-8s | <500ms | **10-16x** |
| `category:*` | 5-8s | <500ms | **10-16x** |
| `model:*` | 5-8s | <500ms | **10-16x** |
| Text prompt | 10-15s | 10-15s | Без изменений |

### User Experience

- **До:** Заметная задержка при каждом клике (раздражает)
- **После:** Мгновенный отклик (как в обычных Telegram ботах)

---

## Безопасность

✅ **Кеш только для UI** — никакой генерации контента  
✅ **Fallback в Claude** — если кеш недоступен, используется обычный путь  
✅ **Валидация команд** — только `/gen`, `category:*`, `model:*`, `back:*`  
✅ **Не влияет** на другие команды Clawdbot  
✅ **Откат** за 10 секунд через `rollback.sh`

---

## Troubleshooting

### Кнопки всё равно медленные

```bash
# 1. Проверь что патч применён
grep "GEN FAST PATH" ~/.nvm/versions/node/v22.22.0/lib/node_modules/clawdbot/dist/telegram/bot-handlers.js

# 2. Проверь что кеш загружается
cat ~/.clawdbot/extensions/gen-fast-path/gen-cache.json | jq keys

# 3. Проверь логи
journalctl --user -u clawdbot-gateway --since '5 minutes ago'

# 4. Запусти verify
bash scripts/verify.sh
```

### Clawdbot не стартует

```bash
# Откат
bash scripts/rollback.sh

# Проверь синтаксис
node -c ~/.nvm/versions/node/v22.22.0/lib/node_modules/clawdbot/dist/telegram/bot-handlers.js
```

### Новые модели не появляются

```bash
# Обновить кеш
bash scripts/update-cache.sh

# Перезапустить
systemctl --user restart clawdbot-gateway
```

---

## Changelog

### v1.0.0 (2026-01-24)

- ✨ Первая версия
- ⚡ Кеширование 23 UI команд
- 🚀 Улучшение производительности 10-16x
- 🔧 Автоматическая установка через `apply-patch.sh`
- 📦 Скрипты обновления и отката
- ✅ Проверка установки через `verify.sh`

---

## Credits

- **Разработка:** [@ChipCR](https://t.me/ChipCR)
- **Интеграция:** Claude (Anthropic)
- **Архитектура:** Bot Proxy → Direct Patch
- **Skill Framework:** Clawdbot

---

## License

MIT

---

## Support

Issues: https://github.com/evgyur/clawdbot/issues

Telegram: [@ChipCR](https://t.me/ChipCR)
