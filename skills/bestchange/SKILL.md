---
name: bestchange
description: BestChange.ru курсы обмена. `/cash` — USDT TRC20 → RUB наличные Москва (топ-10 средняя + диапазон).
metadata:
  clawdbot:
    emoji: "💸"
    requires:
      bins: ["python3"]
---

# BestChange Exchange Rates

Получение актуальных курсов обмена с BestChange.ru.

## Команды

### `/cash`
USDT TRC20 → RUB наличные Москва
- Средняя цена по топ-10 обменникам
- Диапазон (мин/макс)
- Ссылка на BestChange

## Использование

Для команды `/cash`:

```bash
{baseDir}/venv/bin/python3 {baseDir}/scripts/bestchange.py cash
```

Использует Playwright для рендеринга страницы и извлечения курсов.

Вернёт JSON с полями:
- `average` — средняя цена топ-10
- `min` — минимальная в топ-10
- `max` — максимальная в топ-10
- `count` — сколько обменников
- `url` — ссылка на BestChange
- `text` — форматированный вывод

## Расширение

Для других направлений добавить команды:
```bash
python3 {baseDir}/scripts/bestchange.py <command> <from> <to> <city>
```

Примеры:
- `usdt-rub-card` — USDT TRC20 → RUB карта
- `btc-rub-cash` — BTC → RUB наличные
